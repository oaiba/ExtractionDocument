---
description: Always run Hugo server in development mode to preview changes.
---

When editing existing Markdown documents or creating new ones, ALWAYS run the command `hugo server -D` to verify the changes and ensure the site builds correctly without any missing icons, broken links, or syntax errors. Wait a few seconds to verify the build output, and if the site builds successfully, you can terminate the server and proceed.
